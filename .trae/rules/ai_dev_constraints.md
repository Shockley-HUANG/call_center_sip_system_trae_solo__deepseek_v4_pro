# AI 永久开发约束规则（全局固化）

## 核心原则
1. 严格增量开发，禁止全项目复盘、全局重梳理
2. 拒绝冗余操作，不重复解读旧文档、不重复校验已成型环境
3. 任务精准聚焦，接收指令直接进入核心开发
4. 规范永久沿用，所有版本迭代规则、Git约束、开发流程永久生效
5. 启用 tasks.md 任务清单管控，作为唯一项目进度依据
6. 固化 Spec 项目规范机制，不重复解读基础规范内容
7. 报错中断自动续跑，仅接续剩余未完成逻辑

## 生效范围
全项目永久，无需每次重新阅读理解。
